server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: unknown paths serve index.html so client-side
    # routing works instead of returning 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
